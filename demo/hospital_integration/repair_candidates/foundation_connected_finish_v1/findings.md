# Foundation finish on the fixed connected-room geometry

User authorized applying the human-approved Foundation finish to the coherent connected-room study. This is a reference adaptation of that material language, not approval or replacement of production architecture. All walls retain the study's full 44px height; the production low-front camera policy remains unresolved.

## Source and mapping

Existing refresh candidates in `../foundation_wall_refresh_family_v1/` supply all finish colors and plaster pixels. Exact hashes for the back, left and right sources are recorded in `source_hashes.json`. The back-wall source is the human-approved V2 visual language; its propagated family remains reference-only.

- The 44px wall face samples back-source rows 8–51 directly as `row = 51 - height_above_ground`, preserving separator, warm ivory plaster, teal base and lower outline. Horizontal sampling repeats at the original 32px stride, without scaling or repainting.
- The 8px cap uses the back-source profile. Its upper/left distance is measured from the connected footprint's exposed boundary, so shared T/cross interiors acquire no tile borders. This adapts material shading to the existing geometry; it does not copy rendered directional sprites by rotation.
- Narrow jamb shading samples each independently authored left/right source's face color. Exterior edges retain the source outline colors.

Only two default-no-op material hooks were added to the shared renderer. `foundation_reference_rooms.gd` inherits the connected-room footprint, all heights, actor positions, collision and movement. Both comparison panels show identical furniture and figure poses; left is plain construction and right has the finish. The earlier connected scene was rerun and its tracked images remain byte-identical.

## Visual assessment

Codex inspected `corridor_godot.png` and the native doorway view. Blue-slate cap highlights turn continuously through corners and the T/cross. Warm ivory plaster is quiet and the narrow teal base reads consistently across runs, terminating at the door openings. No repeated vertical tile borders are visible. The southern divider's exposed terminal remains a plain narrow full-height face; its geometry was deliberately not altered during this material task.

This is ready for human visual review as a finished-material candidate. Full hospital integration is still outstanding. The full-front visibility tradeoff remains visible and has not been silently resolved by changing the camera contract. Production remains **224 manifest / 220 approved / 4 pending**.

## Reproduce

Open `../../foundation_reference_rooms.tscn` with F6. WASD/F/G/R controls remain.

```powershell
& C:/Godot/Godot_v4.7.1-stable_win64_console.exe --path demo/hospital_integration --log-file .qa/foundation_connected_run.log res://foundation_reference_rooms.tscn --quit-after 240 -- --foundation-room-review
```

Expect `FOUNDATION_CONNECTED_FINISH_PASS`: material differences must stay inside projected wall columns, selected source face/base band values must match across a repeat boundary, and the existing room/corridor/exit walkthrough and figure occlusion must still pass. Captures cover rooms, corridor, rear-wall, doorway and exit poses plus an empty native view. An initial sample check accidentally overlapped the bed and correctly failed; it was moved to an unobstructed part of the same wall band before the final pass.

Verification: Godot finish checks and connected-room regression passed. Focused integration tests **10 passed in 0.38s**; full suite once **379 passed in 56.88s**. Existing preservation baseline confirms **3,533 protected files unchanged**, including production, metadata, reference downloads and the five user-owned demo files. Earlier connected-room captures were regenerated byte-identically; source finish files are unchanged. `git diff --check` passed.
